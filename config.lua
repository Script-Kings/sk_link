Link = {}

Link = {
    debug = false,
}

-- Framework Settings
-- Options: 'auto', 'esx', 'qbcore', 'qbox', 'ox', 'standalone',
Link.framework = 'auto'

-- Inventory Settings
-- Options: 'auto', 'ox_inventory', 'qs-inventory', 'ps-inventory', 'codem-inventory'
Link.inventory = 'auto'

-- Notification Settings
-- Options: 'framework', 'ox', 'codem-notification', 'okokNotify', 'mythic', '17mov', 'standalone'
Link.notifications = 'ox'

-- Input Settings
-- Options: 'ox_target', 'qb-target', 'qtarget', 'interact'
Link.input = 'ox_target'

-- Radial Settings
-- Options: 'ox', 'standalone'
Link.radial = 'ox'

-- Police Dispatch Settings
-- Options: 'standalone', 'ps-dispatch'
Link.dispatch = 'auto'
